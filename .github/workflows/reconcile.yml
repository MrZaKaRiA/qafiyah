name: weekly-reconcile

on:
  schedule:
    - cron: "0 4 * * 0"
  workflow_dispatch:

permissions: {}

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Trigger worker reconciliation
        run: |
          curl -fsS -X POST "${{ secrets.WORKER_RECONCILE_URL }}" \
            -H "Authorization: Bearer ${{ secrets.RECONCILE_TOKEN }}" \
            --max-time 60
